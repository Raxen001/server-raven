services:
  mc:
    image: itzg/minecraft-server:latest
    tty: true
    stdin_open: true
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      TYPE: "PAPER"
      PAPER_CHANNEL: "experimental"
      MEMORY: 5G
    volumes:
      - "/opt/backup/linuxgsm/minecraft:/data"

  # linuxgsm-killingfloor2:
  #   image: gameservermanagers/gameserver:kf2
  #   restart: unless-stopped
  #   volumes:
  #     - ~/linuxgsm/kf2:/data
  #   network_mode: host

