services:
  caddy:
    image: caddy:latest
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./conf:/etc/caddy
      - caddy_data:/data
      - caddy_config:/config
    environment:
      - "DOMAIN=raxen001.xyz"
    extra_hosts:
      - "host.docker.internal:192.168.1.4"

volumes:
  caddy_data:
  caddy_config:
